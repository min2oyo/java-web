load-on-startup
- init()을 실행할 때 시간이 길어질 수 밖에 없는 단점을 보완

특징
- 톰캣 컨테이너가 실행되면서 미리 서블릿을 실행
- 지정한 숫자가 0보다 크면 톰캣 컨테이너가 실행되면서 서블릿이 초기화 됨
- 지정한 숫자는 우선순위를 의미하며 작은 숫자부터 먼저 초기화 됨
- 우선순위는 양의 정수로 지정하며 숫자가 작으면 높음

초기화
- '@WebServlet 애너테이션' 사용
- 'web.xml' 사용

연결
- WEB-INF - web.xml - <servlet>
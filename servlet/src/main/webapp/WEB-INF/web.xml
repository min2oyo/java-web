<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://xmlns.jcp.org/xml/ns/javaee" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd" id="WebApp_ID" version="4.0">
	<display-name>servlet</display-name>
	<welcome-file-list>
		<welcome-file>index.html</welcome-file>
		<welcome-file>index.jsp</welcome-file>
		<welcome-file>index.htm</welcome-file>
		<welcome-file>default.html</welcome-file>
		<welcome-file>default.jsp</welcome-file>
		<welcome-file>default.htm</welcome-file>
	</welcome-file-list>

	<!-- mapping -->
	<servlet>	<!-- 브라우저에서 요청하는 매핑 이름에 대해 실제로 실행하는 서블릿 클래스를 설정하는 태그 -->
		<servlet-name>a</servlet-name>	<!-- <servlet-mapping>태그의 <servlet-name> 태그와 값이 동일 -->
		<servlet-class>mapping.XmlA</servlet-class>	<!-- 브라우저에서 요청하는 매핑 이름에 대해 실제로 기능을 수행하는 서블릿 클래스를 설정 -->
	</servlet>
	<servlet>
		<servlet-name>b</servlet-name>
		<servlet-class>mapping.XmlB</servlet-class>
	</servlet>
	
	<servlet-mapping>	<!-- 브라우저에서 요청하는 논리적인 서블릿을 설정 -->
		<servlet-name>a</servlet-name>	<!-- 매핑 이름으로 요청 시 같은 <servlet> 태그 안의 <servlet-name> 태그와 연결됨 -->
		<url-pattern>/firstMapping</url-pattern>	<!-- 브라우저에서 <servlet-class>를 요청하는 논리적인 서블릿 이름 -->
	</servlet-mapping>
	<servlet-mapping>
		<servlet-name>b</servlet-name>
		<url-pattern>/secondMapping</url-pattern>
	</servlet-mapping>
	
	<!-- 	loadOnStartup -->
	<servlet>
		<servlet-name>loadConfig</servlet-name>	<!-- 	애너테이션으로 서블릿 생성 시 name 속성 값으로 설정 -->
		<servlet-class>api.loadOnStartup.LoadAppConfig</servlet-class>	<!-- 	패키지 이름까지 포함된 서블릿 클래스 이름을 설정 -->
		<load-on-startup>1</load-on-startup>	<!-- 	우선순위 -->
	</servlet>
	
		<servlet-mapping>
		<servlet-name>loadConfig</servlet-name>
		<url-pattern>/loadConfig</url-pattern>
	</servlet-mapping>

	<!-- ContextParamServlet -->
	<context-param>	<!-- 초기값 설정 -->
		<param-name>menuMember</param-name>
		<param-value>회원등록 회원조회 회원수정</param-value>
	</context-param>

	<context-param>
		<param-name>menuOrder</param-name>
		<param-value>주문조회 주문등록 주문수정 주문취소</param-value>
	</context-param>

	<context-param>
		<param-name>menuGoods</param-name>
		<param-value>상품조회 상품등록 상품수정 상품삭제</param-value>
	</context-param>

</web-app>